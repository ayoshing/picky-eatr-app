<h1>Preferences New Page</h1>

<%= form_for @preference do |f| %>
<%= f.collection_check_boxes :cuisine_ids, Cuisine.all, :id, :name %><br>
<%= f.collection_select :user_id, User.all, :id, :name %><br>
<%= f.submit "Update Preferences" %>
<% end %>
