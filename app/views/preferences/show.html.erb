<% if @cuisines_preferences.length > 0 %>

<%= form_for @cuisines_preference do |f| %>
<%= f.collection_radio_buttons :cuisine_id, session[:matches], :id, :name %>
<%= f.submit "Pick"%>
<%end%>
<% else %>

<%= Cuisine.find(session[:selected_cuisine_id]).name %>

<% end %>
